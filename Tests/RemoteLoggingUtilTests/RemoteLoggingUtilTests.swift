import XCTest
@testable import RemoteLoggingUtil

final class RemoteLoggingUtilTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RemoteLoggingUtil().text, "Hello, World!")
    }
}
